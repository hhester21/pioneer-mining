import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import ClientBody from "./ClientBody";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Pioneer Mining - Pioneering the Future of Bitcoin and Energy",
  description: "Leading Bitcoin mining through focused partnerships and efficient operations. 50 MW facility with institutional-grade digital asset infrastructure and Fortune 100 power partnerships.",
  keywords: "Bitcoin mining, cryptocurrency mining, institutional mining, digital assets, Bitcoin investment, mining facility, renewable energy mining",
  authors: [{ name: "Pioneer Mining" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body suppressHydrationWarning className="antialiased">
        <ClientBody>{children}</ClientBody>
      </body>
    </html>
  );
}
